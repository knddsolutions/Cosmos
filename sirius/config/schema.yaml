externalApis:
    centerRegister:
        apiUri: "center/registration"
        supportedMethods:
            POST: "Open"
            GET: "USER"
        params:
            Name:
                required: True
                read-only: False
                type: "str"
                hidden: False
            Email:
                required: True
                read-only: False
                type: "str"
                hidden: False
            Phone:
                required: True
                read-only: False
                type: "str"
                hidden: False
            Platform:
                required: True
                read-only: False
                type:
                  - "BowlingCenter"
                hidden: False
            MemberID:
                required: True
                read-only: True
                type: "str"
                hidden: True
    loyaltyPoints:
        apiUri: "center/loyalty/points"
        supportedMethods:
            PATCH: "USER"
            GET: "USER"
        params:
            Points:
                required: True
                read-only: False
                type: "int"
                hidden: False
    approveCenter:
        apiUri: "center/confirmed"
        supportedMethods:
            POST: "ADMIN"
        params:
            MemberID:
                required: True
                read-only: True
                type: "str"
                hidden: True
            Path:
                required: True
                read-only: True
                type: "str"
                hidden: True
    declineCenter:
        apiUri: "center/declined"
        supportedMethods:
            POST: "ADMIN"
        params:
            MemberID:
                required: True
                read-only: True
                type: "str"
                hidden: True
    suspendServices:
        apiUri: "center/suspend-services"
        supportedMethods:
            POST: "ADMIN"
        params:
            MemberID:
                required: True
                read-only: True
                type: "str"
                hidden: True
            
    centerMembers:
        apiUri: "center/members"
        supportedMethods:
            GET: "USER"
            POST: "USER"
        params:
            




